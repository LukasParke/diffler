{
  "name": "diffler-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for diffler CLI, stats-action engine, and remotion renderer",
  "type": "module",
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "verify": "pnpm build && pnpm typecheck && pnpm test",
    "test:package": "node scripts/test-package.mjs",
    "diffler": "pnpm --filter @lukasparke/diffler diffler",
    "render": "pnpm --filter @lukasparke/diffler-remotion render"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^4.1.8"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
